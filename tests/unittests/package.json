{
  "name": "linq-to-typescript-test",
  "version": "1.0.0",
  "description": "Test",
  "main": "jest.config.js",
  "private": true,
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "jest -c ./jest.config.js --rootDir tests --reporters=jest-fixed-default-reporter/DefaultReporter --reporters=jest-fixed-default-reporter/SummaryReporter"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "linq-to-typescript": "file:../../dist",
    "ts-jest": "^29.2.5"
  },
  "devDependencies": {
    "jest-fixed-default-reporter": "^2.0.1",
    "typescript": "^5.6.3"
  }
}
